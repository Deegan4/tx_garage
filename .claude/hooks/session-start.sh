#!/usr/bin/env bash
echo '{"systemMessage": "tx_garage — no build step. Reload: restart tx_garage. Toggle debug: Config.Debug=true. Known bug: JS posts garage/valet but Lua registers valet/request."}'
